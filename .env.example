# Archivo de ejemplo para configuración del Sistema de Gestión de Ausencias
# Copia este archivo a .env y completa con tus valores reales

# Token del bot de Telegram (obtenido de @BotFather)
TELEGRAM_TOKEN=tu_token_aqui

# URL de la base de datos (SQLite por defecto)
DATABASE_URL=sqlite:///sistema_ausencias.db

# Nivel de logging
LOG_LEVEL=INFO

# Puerto del dashboard web (opcional, por defecto 8090)
WEB_PORT=8090

# NOTA: Los archivos service_account.json y client_secret.json deben
# colocarse en el directorio raíz para funcionalidad de Google Drive
# Estos archivos están excluidos por .gitignore por seguridad